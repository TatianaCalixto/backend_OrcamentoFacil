# ============================================================
# OrçaFácil — Backend / Variáveis de ambiente (exemplo)
# Copie este arquivo para .env e ajuste para o seu ambiente.
# NUNCA commitar o .env real.
# ============================================================

# ----- PostgreSQL (consumido por docker-compose.yml) -----
POSTGRES_USER=orcafacil
POSTGRES_PASSWORD=trocar_em_producao
POSTGRES_DB=orcafacil
POSTGRES_PORT=5432

# ----- Backend FastAPI (será usado a partir da Sprint 1) -----
# DATABASE_URL=postgresql+psycopg://orcafacil:trocar_em_producao@localhost:5432/orcafacil
# JWT_SECRET=defina_um_segredo_forte_aqui
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_MINUTES=60
# ENVIRONMENT=development
