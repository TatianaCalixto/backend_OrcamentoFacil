[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -ra --strict-markers
# Cobertura minima de 80% sera exigida via --cov-fail-under=80 a partir
# da Sprint 10 (S10-T**: hardening do backend). Por enquanto pytest-cov
# esta apenas instalado; rodar como: pytest --cov=app --cov-report=term-missing
