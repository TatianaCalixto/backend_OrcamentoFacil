# OrçaFácil — Backend / Dependências
# Python 3.12+ obrigatório.

# --- Runtime ---
fastapi>=0.136.3
uvicorn>=0.49.0
python-multipart>=0.0.32  # para multipart/form-data (upload de CSV em S09)
slowapi>=0.1.9  # rate limiting (S10-T02)
prometheus-fastapi-instrumentator>=8.0.0  # /metrics Prometheus (S21-T05)
sqlalchemy>=2.0.50
greenlet>=3.5.1  # exigido pelo modo async do SQLAlchemy (S24-T01)
alembic>=1.18.4
psycopg[binary]>=3.3.4  # suporta driver async (postgresql+psycopg) usado pelo AsyncEngine
pydantic>=2.13.4
pydantic-settings>=2.14.1
passlib[bcrypt]>=1.7.4
bcrypt>=5.0.0,<5.1  # passlib 1.7.4 quebra com bcrypt >= 4.1 (sem .__about__)
python-jose>=3.5.0

# --- Testes ---
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
httpx>=0.28.1
aiosqlite>=0.22.1  # driver SQLite async para a suite de testes (S24-T01)
requests>=2.34.2  # usado por scripts/smoke_prod.py (S16-T06)

# --- Lint / Format ---
ruff>=0.15.16
black>=26.5.1

# --- Segurança (CI) ---
pip-audit>=2.10.0  # varredura de CVE; gate por severidade em scripts/ci_pip_audit.py (S20-T02)
