# OrçaFácil — Backend / Dependências
# Python 3.12+ obrigatório.

# --- Runtime ---
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9  # para multipart/form-data (upload de CSV em S09)
slowapi>=0.1.9  # rate limiting (S10-T02)
prometheus-fastapi-instrumentator>=7.0  # /metrics Prometheus (S21-T05)
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic>=2.7
pydantic-settings>=2.4
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1  # passlib 1.7.4 quebra com bcrypt >= 4.1 (sem .__about__)
python-jose>=3.3

# --- Testes ---
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27
requests>=2.32  # usado por scripts/smoke_prod.py (S16-T06)

# --- Lint / Format ---
ruff>=0.6
black>=24.8

# --- Segurança (CI) ---
pip-audit>=2.7  # varredura de CVE; gate por severidade em scripts/ci_pip_audit.py (S20-T02)
