# OrçaFácil — Backend / Dependências
# Python 3.12+ obrigatório.

# --- Runtime ---
fastapi>=0.136.3
uvicorn>=0.48.0
python-multipart>=0.0.29  # para multipart/form-data (upload de CSV em S09)
slowapi>=0.1.9  # rate limiting (S10-T02)
sqlalchemy>=2.0.50
alembic>=1.18.4
psycopg[binary]>=3.3.4
pydantic>=2.13.4
pydantic-settings>=2.14.1
passlib[bcrypt]>=1.7.4
bcrypt>=5.0.0,<5.1  # passlib 1.7.4 quebra com bcrypt >= 4.1 (sem .__about__)
python-jose>=3.5.0

# --- Testes ---
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
httpx>=0.28.1
requests>=2.34.2  # usado por scripts/smoke_prod.py (S16-T06)

# --- Lint / Format ---
ruff>=0.15.15
black>=26.5.1
